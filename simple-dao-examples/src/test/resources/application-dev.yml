spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/oak_base
    username: postgres
    password: DbPwd2026

  jpa:
    show-sql: true
    database: postgresql
    generate-ddl: true
    properties:
      hibernate.query.hql.json_functions_enabled: true
    hibernate:
      ddl-auto: update
      naming:
        physical-strategy: com.levin.commons.dao.support.EntityNamingStrategy
