#Auto gen by simple-dao-codegen ${.now}
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/${moduleName}
    username: prod
    password: prod

  jpa:
    show-sql: false
    generate-ddl: true
    database: MYSQL     # DB2, DERBY,  H2,  HANA,  HSQL,  INFORMIX,   MYSQL,  ORACLE,  POSTGRESQL, SQL_SERVER,   SYBASE;
