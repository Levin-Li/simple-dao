#@author Auto gen by simple-dao-codegen, @time: ${.now}, 代码生成哈希校验码：[]，请不要修改和删除此行内容。

spring:
  datasource:

    #h2 内存数据库
#    url: jdbc:h2:tcp://localhost:9092/mem:dev;MODE=MySQL;

    #h2 文件数据库，项目位置的文件数据库，默认会自动数据库，如果想删除库，直接删除项目位置下的dev.mv.db文件即可
    url: jdbc:h2:tcp://localhost:9092/dev;MODE=MySQL;

    username: dev
    password: dev

  jpa:
    show-sql: true
    generate-ddl: true
    database: mysql
    hibernate:
      ddl-auto: update

