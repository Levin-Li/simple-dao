#
#默认 .env 仅作用于 Compose 模板，不会自动注入到容器内 仅在解析 docker compose.yml 模板时生效（替换 ${变量}）
#
JDK_VERSION=21

#JAVA_OPTS
#Spring load 参数
JAVA_OPTS="-server -Xms384m -Xmx4g -Dloader.path=config,resources,biz-libs,common-biz-libs,third-libs,common-third-libs,"
