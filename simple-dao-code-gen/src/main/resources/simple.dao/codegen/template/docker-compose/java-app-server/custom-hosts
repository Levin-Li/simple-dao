# 文件内容需遵循标准的 /etc/hosts 格式

# 每行格式：IP地址  域名1 域名2 ...
# 192.168.1.100  api.example.com  api
# 172.18.0.5     db-master        mysql
# 10.0.0.1       cache-server     redis

#通一个主机名称映射多个IP时, 在Linux/macOS按hosts 文件中出现的顺序，第一个有效IP被使用.

######################################
127.0.0.1 localhost
#######################################
